const config = {
  appName: "Demega Spare Parts Buyer MVP 2026",
  version: "0.1.0",
  environment: process.env.NODE_ENV || "development",
  port: Number(process.env.PORT || 3000)
};

module.exports = config;
